node_modules
.next
.turbo
.env
.env.local
*.log
.DS_Store
packages/contracts/build
packages/contracts/Published.toml
**/data/*.db
/skills-main
plan.md

# DeepBook dependencies (build artifacts)
deps/deepbookv3/**/build/
deps/deepbookv3-self-hosted/**/build/

# DeepBook cloned repo (use as submodule or clone separately)
deps/deepbookv3/
deps/deepbookv3-self-hosted/

# Sui build outputs
*.mv
!*.move

# Local deployment outputs
.env.deployed*
.vercel

# Compiled output
**/tsconfig.tsbuildinfo

# Agents service dist (large; rebuilt on every deploy).
# The SDK dist is still tracked so consumers can install
# the prebuilt package; the agents service is only run
# from source via `pnpm dev:agents` or rebuilt on deploy.
apps/agents/dist/

# SQLite temp files
*.db-wal
*.db-shm
*.db-journal

packages/sdk/dist
